import CloudDB from './api.js';

// ─── Cloud-only DB Implementation ────────────────────────────────────────────
// Legacy IndexedDB code wiped to fulfill migration directive.
// The UI panels continue to use this DB export without knowing it's now Cloud.

export default CloudDB;
